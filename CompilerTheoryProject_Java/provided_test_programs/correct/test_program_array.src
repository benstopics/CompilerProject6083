program test_program_array is
    global integer numbers[10];
    global integer res[10];
    global integer i;
    global procedure square_array(integer array[10] in, integer result[10] out)
        integer i;
    begin
        i := 0;
        for (i := i + 1; i < 10)
            result[i] := array[i] * array[i];
        end for;
    end procedure;
begin
    i := 0;
    for (i := i + 1; i < 10)
        numbers[i] := i;
    end for;
    square_array(numbers[0], res[0]);
    i := 0;
    for (i := i + 1; i < 10)
        //putintegereger(res[i]);
        //putString(" ");
    end for;
end program