program test_program is
    global float fibb_result;
	global integer testintarr[10];
    global procedure fibb(integer n in, integer n2 in, float result out, integer anotherresult[2] out)
        float a;
        float b;
		procedure subfibb()
			integer c;
		begin
		
		end procedure;
    begin
        if (n < 0) then
            result := not( not(1));
            return;
        end if;
        if (n == 0) then
            result := 0;
            return;
        end if;
        if (n == 1) then
            result := 1;
            return;
        end if;
        
        fibb(n-1, n- 2, fibb_result, testintarr);
        //fibb(n-2, n -2, b);

        result := a + b;
    end procedure;
	
	global procedure test() begin end procedure;
begin
    //fibb_result := -1234;
    //fibb(12, 5, fibb_result);
    //putInteger(fibb_result);
end program